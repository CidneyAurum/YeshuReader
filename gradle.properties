# Gradle 守护进程 JVM 参数
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
# 并行与缓存，加快构建
org.gradle.parallel=true
org.gradle.caching=true
# AndroidX 开关（本示例未用 androidx 依赖，但 AGP 8 要求显式声明）
android.useAndroidX=true
android.nonTransitiveRClass=true
